<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
		xmlns:beans="http://www.springframework.org/schema/beans"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xmlns:context="http://www.springframework.org/schema/context"
		xmlns:mvc="http://www.springframework.org/schema/mvc"
		xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        https://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/mvc
        https://www.springframework.org/schema/mvc/spring-mvc.xsd">

	<!-- Spring MVC annotation 활성화 -->
	<mvc:annotation-driven />

	<!-- 정적 리소스 처리 -->
	<mvc:resources mapping="/resources/**" location="/resources/" />
	<mvc:resources mapping="/css/**" location="/css/" />
	<mvc:resources mapping="/js/**" location="/js/" />

	<!-- View Resolver -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<beans:property name="suffix" value=".jsp" />
	</beans:bean>

	<!-- Controller / Component Scan -->
	<context:component-scan base-package="project" />

	<!-- 🔥 인터셉터 등록 (핵심) -->
	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/**"/>

			<!-- 정적 리소스 제외 -->
			<mvc:exclude-mapping path="/resources/**"/>
			<mvc:exclude-mapping path="/css/**"/>
			<mvc:exclude-mapping path="/js/**"/>
			<mvc:exclude-mapping path="/images/**"/>

			<beans:bean class="project.util.interceptor.UnreadInterceptor"/>
		</mvc:interceptor>

		<!-- ★ AdminAuthInterceptor 추가 ★ -->
		<mvc:interceptor>
			<mvc:mapping path="/admin/**"/>
			<mvc:exclude-mapping path="/admin/login"/>
			<mvc:exclude-mapping path="/admin/loginProcess"/>
			<beans:bean class="project.config.interceptor.AdminAuthInterceptor"/>
		</mvc:interceptor>

		<!-- ★ MemberActivityInterceptor 추가 ★ -->
		<mvc:interceptor>
			<mvc:mapping path="/**"/>
			<mvc:exclude-mapping path="/admin/**"/>
			<mvc:exclude-mapping path="/css/**"/>
			<mvc:exclude-mapping path="/js/**"/>
			<mvc:exclude-mapping path="/img/**"/>
			<mvc:exclude-mapping path="/api/member/logout-pending"/>
			<mvc:exclude-mapping path="/api/member/cancel-logout"/>
			<beans:bean class="project.config.interceptor.MemberActivityInterceptor"/>
		</mvc:interceptor>
	</mvc:interceptors>

</beans:beans>
