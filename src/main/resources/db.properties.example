# Database Configuration Example
# 이 파일을 복사해서 db.properties로 만들고 실제 값을 입력하세요
# Copy this file to db.properties and fill in your actual database credentials

# PostgreSQL Driver (log4jdbc를 사용하여 SQL 로깅)
db.driver=net.sf.log4jdbc.sql.jdbcapi.DriverSpy

# Database URL (AWS RDS PostgreSQL)
# 형식: jdbc:log4jdbc:postgresql://[호스트]:[포트]/[데이터베이스명]
db.url=jdbc:log4jdbc:postgresql://your-database-host.rds.amazonaws.com:5432/your-database-name

# Database Username
db.username=your-username

# Database Password
db.password=your-password
